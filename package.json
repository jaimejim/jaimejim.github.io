{
  "name": "jaime-win",
  "version": "1.0.0",
  "private": true,
  "description": "Tooling-only manifest for the jaime.win Jekyll blog (link + performance checks).",
  "scripts": {
    "lhci": "lhci autorun"
  },
  "devDependencies": {
    "@lhci/cli": "^0.15.1"
  }
}
